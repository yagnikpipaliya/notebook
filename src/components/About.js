import React,{useContext} from 'react'
import noteContext from '../context/notes/noteContext'

function About() {
  // const a =useContext(noteContext)
  return (
    <div>About </div>
    // {a.state.name} {a.update()}
  )
}

export default About